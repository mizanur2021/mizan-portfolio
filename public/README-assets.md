# Replace these placeholder assets

- `og.png`        → 1200×630 social share image (used by Open Graph / Twitter)
- `icon.png`      → 512×512 app icon (PWA manifest)
- `cv.pdf`        → your downloadable CV (linked from the Resume section)
- Hero/profile image → currently uses a remote avatar in `sections/hero.tsx`;
  swap the `src` for your own photo placed in `/public` (e.g. `/profile.jpg`).

Remote images (Unsplash / pravatar) are placeholders for the portfolio grid and
testimonials. Replace them with your real client work and client photos.
